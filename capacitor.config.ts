import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.mappaterritori.app',
  appName: 'MappaTerritori',
  webDir: 'dist',
  android: {},
}

export default config
